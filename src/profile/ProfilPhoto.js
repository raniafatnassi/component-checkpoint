import React from "react";
import profilePictute from '../profile.png';

const ProfilPhoto = () => {
    return(
        <img className = 'profilePhoto' src={profilePictute} />
    );
}

export default ProfilPhoto;
